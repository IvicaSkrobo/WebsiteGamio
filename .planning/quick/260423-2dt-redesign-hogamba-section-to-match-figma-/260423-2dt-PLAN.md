---
phase: quick
plan: 260423-2dt
type: execute
wave: 1
depends_on: []
files_modified: [app/page.tsx]
autonomous: true
requirements: [desktop-fidelity-hogamba]

must_haves:
  truths:
    - "Hogamba section shows a single static layout (not a carousel)"
    - "Left panel renders a rocket game CSS approximation with multiplier, win toast, and balance"
    - "Right panel renders a balloon mascot CSS approximation"
    - "Bottom row shows PLACE BET (green) and CASHOUT (orange) buttons with auto-bet/cashout labels"
    - "TypeScript compiles with zero errors — no unused refs, no missing identifiers"
  artifacts:
    - path: "app/page.tsx"
      provides: "Updated Hogamba section + two new fallback components"
      contains: "HogambaGameFallback, HogambaMascotFallback"
  key_links:
    - from: "magarba* refs (removed)"
      to: "GSAP mm.add block"
      via: "null-guarded if block — must be fully deleted, not just emptied"
---

<objective>
Replace the three-slide Hogamba carousel with a single-view static layout matching the Figma design. Remove all dead carousel scaffolding (refs, const, GSAP block, unused fallback component) so TypeScript compiles cleanly. Add two CSS fallback components for the game panel and mascot.

Purpose: The carousel was a placeholder that misrepresents the Figma design. The actual Hogamba section is a static split-panel view.
Output: Updated app/page.tsx with clean Hogamba section, two new CSS fallback components, and no dead code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key line numbers in app/page.tsx for surgical edits. Extracted from full file read. -->

magarbaSlides const:          lines 205–221  (delete entire block)
magarbaSectionRef ref:        line 693       (delete)
magarbaCopyRef ref:           line 694       (delete)
magarbaTrackRef ref:          line 695       (delete)
magarbaProgressRef ref:       line 696       (delete)
GSAP magarba block:           lines 869–915  (delete — the if block checking magarbaSectionRef; arena block at 917–963 must be preserved)
OriginalsPreviewFallback fn:  lines 426–470  (delete entire function — defined but never called)
Hogamba section JSX:          lines 1224–1393 (replace entirely)

The section tag opening at line 1224:
  <section id="hogamba" ref={magarbaSectionRef} className="...">
  After edit the ref={magarbaSectionRef} attribute is gone (ref deleted above).

HogambaLogoFallback at lines 310–318 — keep as-is, still used by hogambaLogoAsset AssetImage.

mm.add block structure (lines 868–964):
  mm.add("(min-width: 1024px)", () => {
    if (magarbaSectionRef...) { ... }   ← lines 869–915: DELETE this entire if block
    if (arenaSectionRef...) { ... }     ← lines 917–963: KEEP
  });
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead carousel scaffolding</name>
  <files>app/page.tsx</files>
  <action>
Make four targeted deletions in app/page.tsx:

1. Delete `magarbaSlides` const (lines 205–221). This is the ShowcaseSlide[] array with label/title/body for 3 slides. Remove the entire const declaration including the closing semicolon.

2. Delete the four magarba useRef declarations (lines 693–696):
   - `const magarbaSectionRef = useRef<HTMLElement | null>(null);`
   - `const magarbaCopyRef = useRef<HTMLDivElement | null>(null);`
   - `const magarbaTrackRef = useRef<HTMLDivElement | null>(null);`
   - `const magarbaProgressRef = useRef<HTMLDivElement | null>(null);`
   Leave the arena refs (lines 697–700) intact.

3. Delete the GSAP magarba if-block inside `mm.add("(min-width: 1024px)")` (lines 869–915). This is the block:
   ```
   if (
     magarbaSectionRef.current &&
     magarbaTrackRef.current &&
     ...
   ) { gsap.timeline({...}) ... }
   ```
   Delete from the opening `if (` through the closing `}` on line 915. The empty line before the arena if-block (line 916) can remain or be removed — either is fine. Do NOT touch the arena if-block.

4. Delete `OriginalsPreviewFallback` function (lines 426–470). This is defined but never called anywhere. Remove the entire function declaration.

After these four deletions the file should have no references to magarba* identifiers and no OriginalsPreviewFallback.
  </action>
  <verify>
    <automated>cd /Users/ivicaskrobo/Documents/GitHub/WebsiteGamio/.claude/worktrees/condescending-rhodes && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript reports zero errors related to magarba* or OriginalsPreviewFallback. No magarba* identifier appears in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add HogambaGameFallback and HogambaMascotFallback components</name>
  <files>app/page.tsx</files>
  <action>
Insert two new component functions directly before the existing `HogambaLogoFallback` function (which starts at the current line after deletions — search for `function HogambaLogoFallback`).

**HogambaGameFallback** — CSS approximation of the rocket game panel:

```tsx
function HogambaGameFallback() {
  return (
    <div className="overflow-hidden rounded-[20px] border border-[rgba(100,200,255,0.15)] bg-[linear-gradient(180deg,#0e1a2a,#070e18)] p-3 shadow-[0_24px_70px_rgba(0,0,0,0.5)]">
      {/* Top bar */}
      <div className="flex items-center justify-between px-2 py-1">
        <div className="flex items-center gap-1.5">
          {[
            "bg-[rgba(255,107,53,0.9)]",
            "bg-[rgba(255,200,80,0.8)]",
            "bg-[rgba(100,220,100,0.8)]",
          ].map((color, index) => (
            <div
              key={`avatar-${index}`}
              className={`h-6 w-6 rounded-full border border-white/20 ${color}`}
            />
          ))}
        </div>
        <div className="flex gap-1">
          {[0, 1, 2].map((dot) => (
            <span key={dot} className="h-1 w-1 rounded-full bg-white/40" />
          ))}
        </div>
      </div>

      {/* Game area */}
      <div className="relative mt-2 overflow-hidden rounded-[16px] bg-[linear-gradient(180deg,#0b1828,#050c18)] p-5">
        {/* Win toast */}
        <div className="absolute left-1/2 top-4 -translate-x-1/2 whitespace-nowrap rounded-full border border-[rgba(93,222,42,0.3)] bg-[rgba(93,222,42,0.12)] px-4 py-1.5 text-[11px] font-bold text-[#8fffaa]">
          You won at 9.85x · €63.00 EUR
        </div>

        {/* Multiplier + rocket */}
        <div className="flex items-end justify-between pt-10 pb-4">
          <div>
            <p className="text-[10px] uppercase tracking-[0.22em] text-white/40">Live round</p>
            <p className="font-display mt-1 text-[3.2rem] font-bold leading-none tracking-[-0.06em]">
              <span className="text-[#5dde2a]">10</span>
              <span className="text-white">.01x</span>
            </p>
          </div>
          {/* Rocket shape */}
          <div className="relative flex h-[80px] w-[40px] flex-col items-center">
            <div className="h-[42px] w-[28px] rounded-t-full bg-[linear-gradient(180deg,#e8f0ff,#8aaad4)]" />
            <div className="h-[24px] w-[28px] bg-[linear-gradient(180deg,#6a8fbf,#3a5a8a)]" />
            <div className="absolute bottom-0 flex gap-1">
              <div className="h-[16px] w-[10px] rounded-br-full bg-[rgba(255,140,60,0.7)] blur-[1px]" />
              <div className="h-[16px] w-[10px] rounded-bl-full bg-[rgba(255,140,60,0.7)] blur-[1px]" />
            </div>
          </div>
        </div>

        {/* Balance */}
        <div className="mt-2 border-t border-white/8 pt-3 text-[12px] text-white/50">
          EUR 468.17
        </div>
      </div>

      {/* Bet row */}
      <div className="mt-2 rounded-[16px] bg-[rgba(255,255,255,0.03)] p-3">
        <div className="flex items-center gap-2">
          {/* Minus/plus input */}
          <div className="flex flex-1 items-center justify-between rounded-[12px] border border-white/10 bg-black/30 px-3 py-2 text-sm font-semibold text-white/80">
            <span className="text-white/50">−</span>
            <span>.00</span>
            <span className="text-white/50">+</span>
          </div>
          <div className="flex flex-1 items-center justify-between rounded-[12px] border border-white/10 bg-black/30 px-3 py-2 text-sm font-semibold text-white/80">
            <span className="text-white/50">−</span>
            <span>1.00</span>
            <span className="text-white/50">+</span>
          </div>
        </div>
        {/* Number grid */}
        <div className="mt-2 grid grid-cols-4 gap-1.5">
          {[1, 2, 3, 4].map((n) => (
            <div
              key={n}
              className="rounded-[10px] border border-white/8 bg-white/[0.05] py-2 text-center text-sm font-bold text-white/70"
            >
              {n}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

**HogambaMascotFallback** — CSS balloon + creature:

```tsx
function HogambaMascotFallback() {
  return (
    <div className="flex flex-col items-center">
      {/* Dome / hot-air balloon */}
      <div className="relative h-[220px] w-[220px] overflow-hidden rounded-t-full border border-[rgba(200,240,255,0.2)] bg-[linear-gradient(180deg,#d8eeff,#b0cfee)]">
        {/* Vertical stripes */}
        {Array.from({ length: 8 }).map((_, index) => (
          <div
            key={`stripe-${index}`}
            className="absolute top-0 h-full w-[2px] bg-black/30"
            style={{ left: `${12 + index * 12}%` }}
          />
        ))}
        {/* Inner glow */}
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_30%,rgba(255,255,255,0.4),rgba(255,255,255,0)_60%)]" />
      </div>
      {/* Creature body */}
      <div className="relative -mt-2 flex h-[72px] w-[72px] items-center justify-center rounded-full border border-[rgba(100,220,200,0.3)] bg-[linear-gradient(180deg,#1a3a3a,#0d2020)]">
        {/* Eyes */}
        <div className="flex gap-4">
          {[0, 1].map((eye) => (
            <div
              key={eye}
              className="h-3 w-3 rounded-full bg-[#00e5c8] shadow-[0_0_8px_rgba(0,229,200,0.8)]"
            />
          ))}
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/ivicaskrobo/Documents/GitHub/WebsiteGamio/.claude/worktrees/condescending-rhodes && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both functions are defined in the file. TypeScript still compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Hogamba section JSX with single-view layout</name>
  <files>app/page.tsx</files>
  <action>
Find the Hogamba section in page.tsx — it starts with `<section id="hogamba"` (search for this string). Replace the entire section from `<section id="hogamba"` through its closing `</section>` with the following JSX.

The old section spans from the opening tag (which had `ref={magarbaSectionRef}` — already removed in Task 1) through the closing `</section>` tag. After Task 1 the opening is `<section id="hogamba" className="relative mx-auto w-full max-w-[1440px] overflow-hidden px-6 py-16 lg:px-0 lg:py-24">`.

Replace that entire section block with:

```tsx
<section
  id="hogamba"
  className="relative mx-auto w-full max-w-[1440px] overflow-hidden px-6 py-16 lg:px-0 lg:py-24"
>
  <div className="pointer-events-none absolute inset-x-0 top-0 h-px bg-white/8" />
  {/* Green radial glow — left-center, cosmic feel */}
  <div className="pointer-events-none absolute left-[-8rem] top-[4rem] h-[36rem] w-[36rem] rounded-full bg-[radial-gradient(circle,rgba(93,222,42,0.22),rgba(93,222,42,0)_68%)] blur-[110px]" />
  <div className="pointer-events-none absolute left-[2%] top-[14rem] h-[22rem] w-[26rem] rounded-full bg-[radial-gradient(circle,rgba(40,180,10,0.16),rgba(40,180,10,0)_75%)] blur-[90px]" />

  <div className="relative mx-auto max-w-[1180px]">
    {/* Logo */}
    <div className="mb-8 flex justify-center lg:mb-10">
      <AssetImage
        src={hogambaLogoAsset}
        alt="Hogamba"
        wrapperClassName="h-8 min-w-[180px]"
        imgClassName="h-full w-full object-contain transition-opacity duration-200"
        fallback={<HogambaLogoFallback />}
      />
    </div>

    {/* Main split: game panel left, mascot right */}
    <div
      data-reveal
      className="mt-8 flex flex-col gap-6 lg:mt-10 lg:flex-row lg:items-center lg:gap-10"
    >
      {/* Left: rocket game panel (~55% width) */}
      <div className="flex-1">
        <HogambaGameFallback />
      </div>

      {/* Right: mascot (~45% width, capped) */}
      <div className="flex justify-center lg:w-[380px] lg:justify-end">
        <HogambaMascotFallback />
      </div>
    </div>

    {/* Bottom: bet controls spanning full width */}
    <div
      data-reveal
      className="mt-6 grid grid-cols-2 gap-4"
    >
      {/* PLACE BET */}
      <div>
        <button
          type="button"
          className="w-full rounded-[14px] bg-[linear-gradient(135deg,#5dde2a,#3aaa10)] py-4 text-center text-[15px] font-bold tracking-[0.12em] text-[#081109] shadow-[0_10px_30px_rgba(93,222,42,0.22)]"
        >
          PLACE BET
        </button>
        <div className="mt-2 flex items-center gap-2 px-1">
          <span className="h-3.5 w-3.5 rounded-full border border-white/30 bg-transparent" />
          <span className="text-[12px] text-white/50">Auto bet</span>
        </div>
      </div>

      {/* CASHOUT */}
      <div>
        <button
          type="button"
          className="w-full rounded-[14px] bg-[linear-gradient(135deg,#ff8c42,#cc5500)] py-4 text-center text-[15px] font-bold tracking-[0.12em] text-white shadow-[0_10px_30px_rgba(255,107,53,0.22)]"
        >
          <span className="block">CASHOUT</span>
          <span className="block text-[13px] font-normal tracking-normal text-white/80">
            23.80 🔒
          </span>
        </button>
        <div className="mt-2 flex items-center gap-2 px-1">
          <span className="h-3.5 w-3.5 rounded-full border border-white/30 bg-transparent" />
          <span className="text-[12px] text-white/50">Auto cashout</span>
        </div>
      </div>
    </div>
  </div>
</section>
```
  </action>
  <verify>
    <automated>cd /Users/ivicaskrobo/Documents/GitHub/WebsiteGamio/.claude/worktrees/condescending-rhodes && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>TypeScript compiles with zero errors. The Hogamba section in page.tsx renders the two-column split layout with PLACE BET and CASHOUT controls. No magarba* references remain in the file. `magarbaSlides`, all four magarba refs, the GSAP magarba block, and OriginalsPreviewFallback are fully absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static site — no trust boundaries | All content is static HTML/CSS/JS. No user input, no auth, no API calls in this section. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hogamba-01 | Tampering | button elements (PLACE BET / CASHOUT) | accept | Buttons are display-only UI in a static marketing site — no handler, no form submission, no state mutation |
</threat_model>

<verification>
After all tasks complete:
1. `npx tsc --noEmit` — zero errors
2. `grep -n "magarba" app/page.tsx` — no output
3. `grep -n "OriginalsPreviewFallback" app/page.tsx` — only the function definition (now deleted), so no output
4. `grep -n "HogambaGameFallback\|HogambaMascotFallback" app/page.tsx` — shows both function definitions and their usage in JSX
5. Visual check: dev server shows Hogamba section with split panel and bet controls
</verification>

<success_criteria>
- Zero TypeScript errors after all three tasks
- No magarba* identifiers anywhere in page.tsx
- OriginalsPreviewFallback removed
- HogambaGameFallback and HogambaMascotFallback defined and rendered in the section
- PLACE BET (green) and CASHOUT (orange) buttons visible below the split panel
- Hogamba section is a single static view — no carousel track, no progress bar, no GSAP pin
</success_criteria>

<output>
After completion, create `.planning/quick/260423-2dt-redesign-hogamba-section-to-match-figma-/260423-2dt-SUMMARY.md`
</output>
