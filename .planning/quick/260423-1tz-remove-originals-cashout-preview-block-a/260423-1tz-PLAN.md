---
phase: quick
plan: 260423-1tz
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - QUICK-260423-1tz
must_haves:
  truths:
    - "Originals section has no cashout preview panel below the article grid"
    - "Hogamba logo appears inside the hogamba section, above the carousel"
  artifacts:
    - path: "app/page.tsx"
      provides: "Updated page with both UI changes applied"
  key_links:
    - from: "Hogamba logo div"
      to: "hogamba section inner container"
      via: "moved inside max-w-[1180px] wrapper, above carousel div"
---

<objective>
Two surgical DOM edits in app/page.tsx:
1. Remove the originals cashout preview block (lines 1221-1233).
2. Move the Hogamba logo div from outside the section into the section's inner content container, above the carousel.

Purpose: Match Figma layout — the cashout panel does not exist in Figma, and the Hogamba logo belongs visually inside the section, not floating before it.
Output: app/page.tsx with both edits applied.
</objective>

<execution_context>
@/Users/ivicaskrobo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivicaskrobo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove originals cashout preview block</name>
  <files>app/page.tsx</files>
  <action>
Delete lines 1221-1233 (inclusive) from app/page.tsx — the entire `<div data-reveal className="relative z-10 mx-auto mt-14 max-w-[1180px] overflow-hidden rounded-[16px] border border-[rgba(255,162,222,0.16)] bg-[#180515] p-2">` block that contains the `AssetImage` referencing `originalsCashoutAsset` and `OriginalsPreviewFallback`. The closing `</div>` at line 1233 is the outer wrapper; remove it too. The `</div>` at line 1233 closes the cashout block, and line 1234 closes the Originals section inner container — do NOT remove line 1234.

After deletion the Originals section should end: articles grid closes → inner container div closes → section closes. No cashout block remains.
  </action>
  <verify>
    <automated>grep -n "originalsCashoutAsset\|OriginalsPreviewFallback" app/page.tsx | wc -l</automated>
  </verify>
  <done>grep returns 0 — neither `originalsCashoutAsset` nor `OriginalsPreviewFallback` appears in the file (or only in variable declarations at the top, not in JSX). The Originals `&lt;section&gt;` closes cleanly after the article grid.</done>
</task>

<task type="auto">
  <name>Task 2: Move Hogamba logo inside the hogamba section</name>
  <files>app/page.tsx</files>
  <action>
The standalone `<div className="mx-auto w-full max-w-[1440px] px-6 py-12 lg:px-0">` wrapper (currently lines 1236-1246, after the Originals section closes) contains the Hogamba logo. Remove this entire outer wrapper div and its inner centering div, keeping only the `AssetImage` + `HogambaLogoFallback` sub-tree.

Place the logo inside `<section id="hogamba">`, inside `<div className="relative mx-auto max-w-[1180px]">`, as the FIRST child — before the `<div data-reveal className="relative overflow-hidden">` carousel div.

Wrap the logo in a simple div: `<div className="mb-8 lg:mb-10 flex justify-center">` (no extra max-width or px padding needed — the parent `max-w-[1180px]` already constrains it).

The result should read:
```jsx
<div className="relative mx-auto max-w-[1180px]">
  <div className="mb-8 lg:mb-10 flex justify-center">
    <AssetImage
      src={hogambaLogoAsset}
      alt="Hogamba"
      wrapperClassName="h-8 min-w-[180px]"
      imgClassName="h-full w-full object-contain transition-opacity duration-200"
      fallback={<HogambaLogoFallback />}
    />
  </div>
  <div data-reveal className="relative overflow-hidden">
    {/* carousel */}
  </div>
</div>
```
  </action>
  <verify>
    <automated>grep -n "hogambaLogoAsset" app/page.tsx</automated>
  </verify>
  <done>The `hogambaLogoAsset` reference appears only once, inside `&lt;section id="hogamba"&gt;`. No standalone outer wrapper div for the logo exists before the section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML output | No user input, no data flow — pure DOM edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | page.tsx edit | accept | Static site, no runtime input; DOM restructure has no security surface |
</threat_model>

<verification>
After both edits:
- `grep -n "originalsCashoutAsset\|OriginalsPreviewFallback" app/page.tsx` — should return 0 JSX hits
- `grep -n "hogambaLogoAsset" app/page.tsx` — should show exactly 1 hit inside the hogamba section
- `npm run build` (or dev server hot reload) should produce no TypeScript or JSX errors
</verification>

<success_criteria>
- Originals section renders article grid only — no cashout preview panel below it
- Hogamba logo renders at the top of the hogamba section inner container, with mb-8/mb-10 spacing above the carousel
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260423-1tz-remove-originals-cashout-preview-block-a/260423-1tz-SUMMARY.md`
</output>
